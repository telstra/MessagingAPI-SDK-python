[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[[source]]
url = "$PYPI_MIRROR"
verify_ssl = true
name = "messaging-pypi"

[packages]
"tls.messaging" = { version="==${VERSION}", index="messaging-pypi" }

[dev-packages]
pytest = "6.2.1"
black = "20.8b1"
pylint = "*"

[scripts]
test = "pytest"

[pipenv]
allow_prereleases = true
